<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Açık Tema -->
    <style name="Theme.RepoSearchApp" parent="Theme.Material3.Light">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        <item name="colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/on_background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/on_error</item>
    </style>

    <style name="Theme.App.RepoSearchApp" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_splash_screen</item> <!-- Arkaplan rengi -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item> <!-- SVG veya PNG logonuz -->
    </style>

</resources>
